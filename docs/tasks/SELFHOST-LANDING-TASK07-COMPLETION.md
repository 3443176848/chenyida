# SELFHOST-LANDING-TASK07 完成报告

## 结论

- 状态：`DONE / OFFLINE WORKBOOK / REVIEW REQUIRED`。
- 固定结论：`OFFLINE STANDARDIZED BOM WORKBOOK CREATED — REVIEW REQUIRED`。
- 已按 `moban.xlsx` 第一张 `原BOM` 到第二张 `Sheet1` 的实际转换关系逐表整理，再合并为一张 `全部物料汇总`；这不是数据库导入、物料审批或生产生效。
- 未连接或写入 PostgreSQL、SQLite、D1，未运行 Migration、build、restart、deploy、push 或 PR。

## 模板与整理规则

- 标准字段逐字固定为：序号、项目号、板子类型、内部型号、物料规格描述、品牌、用量、替代料、供应商、订单数量、需求数量、购买数量、库存数。
- 模板第一张的 53 个主料组、40 条备选料和 2 条板件本体与第二张 53 行目标逐行校验：53/53 有规格身份依据，53/53 用量一致，板件本体按模板不进入采购明细。
- 主料保留一行，同组备选项折叠进 `替代料`；无源器件保留通用电气规格，IC、传感、保护和灯类优先保留关键料号。未知字段留空，不猜测供应商、板型、数量、库存或正式内部编码。
- 相同物料在不同板型或来源中仍分别保留，汇总表不做跨文件模糊去重。A118 一个 42 行完全相同的重复 BOM 区段只保留一次并记录异常。
- 每个来源以当前 BOM/物料主表为准；`更新记录`、`变更记录` 和空 `Sheet3` 是历史或空页，不作为当前采购明细重复汇总，并在工作簿来源说明中明确记录。
- `需求数量` 为 `用量×订单数量`，`购买数量` 为 `max(需求数量-库存数, 0)`；任一必要输入未知时公式结果为空白。

## 输出内容

- 工作文件：`/opt/erp/shujvbiao/全项目标准物料明细.xlsx`；197,821 bytes、root:root `0600`、SHA-256 `aeea74c227f28460c5f39c228268f2ff0b9a09c2b2c5db69ec6aeeb19b271c91`。
- GPT 下载副本：`/mnt/data/晨亿达ERP-全项目标准物料明细.xlsx`；大小、权限和 SHA-256 与工作文件一致。
- `全部物料汇总` 591 行；逐来源为 1928C 22、A118 233、A200 模板量产 BOM 53、A200 物料清单 57、A200 注意事项 0、G20-G15G 56、J587 46、V700 124。
- 150 行带替代料；`来源追溯` 591 行完整对应汇总行；标准页和汇总页共 2,364 个受控公式。
- 另含 `整理异常` 94 条及 `来源与说明`。逐行统计报告位于仓库外 root-only `/var/lib/chenyida-erp/intake/standardized-bom-20260730/report.json`，业务工作簿和报告均不进入 Git。

## 待人工确认

- A200 量产物料清单 57 行没有单机用量，用量保持空白；其原表明确供应商被保留。
- A118 六个只给内部型号的区段共 21 行无法证明板子类型，板型保持空白。
- `moban.xlsx` 与旧 `A200量产BOM.xlsx` 是同一逻辑 BOM，但数量/规格存在 4 处差异；按用户指定的模板为权威，不重复汇总旧版，并逐条记录差异。
- J587 文件名标识 `SUBA2`，表内标题标识 `SUBA1`；按表内真实标题填写并保留版本冲突。
- 9 条 PCB/PCBA/空板本体按模板规则未进入采购明细；A200 注意事项 XLS 是说明档，只保留空标准页。

## 验证与安全

- 专项合成/合同测试 7/7、既有内部物料库导出回归 3/3、Python `py_compile`、`server.py --self-test`、`smoke_test.py`、临时 SQLite `go_live_check.py --no-backup` 全部通过；临时 SQLite 目录已精确清理。
- Node 使用一次一个、断网、只读挂载、1 CPU/1 GiB 的自动删除容器：`npm test` 3/3，lint 0 error，credentials 1,076 个 Git 文件通过。首次 credentials 只挂载 Site 子目录，因容器内看不到根 `.git` 在扫描前停止；改为只读挂载根仓库后通过。
- 工作簿 ZIP 完整、openpyxl 可重新打开、13 列合同与工作表计数通过；宏、外部链接、来源公式注入、电话样式和敏感连接/凭据均为 0。来源 manifest 和模板处理前后摘要、metadata 均不变。
- 最终 Git 只纳入通用离线工具、合成测试和脱敏文档。范围检查时出现的两个并行未跟踪脚本/测试文件保持原样，未读取为交付依据、未暂存或提交；`shujvbiao/` 业务数据继续由 `.gitignore` 保护。

## 资源与清理

- 起点约 2.4 GiB available、Swap 47 MiB、根盘可用 33 GiB、Load `0.36/0.24/0.17`；最终约 2.3 GiB、47 MiB、33 GiB、Load `0.34/0.69/0.56`。
- PostgreSQL/Web/Worker/Caddy 全程未重建，restart 0、OOMKilled false；内核任务时段 OOM 记录 0，四个受保护持久卷保持。
- 测试容器均设置自动删除；任务专用临时 SQLite 已清理。未执行 prune、缓存清理或服务重启。
