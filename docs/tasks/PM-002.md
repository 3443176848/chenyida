# PM-002 补全晨亿达 ERP 多智能体研发系统执行设计

> 状态：`DONE / DESIGN COMPLETE / IMPLEMENTATION NOT STARTED`
> 日期：2026-08-11（Asia/Shanghai）
> 起点：`main@2c8f8b2e224e4a9b0a2ec9e01a5998898ff95aaf`
> 责任：Codex负责现场核验、设计、Markdown验证、治理同步和独立commit；项目负责人负责后续D-114与任何实施阶段选择

## 1. 授权目标

基于`/opt/erp`真实仓库状态，补全ERP专属多智能体研发系统的可执行设计。覆盖角色、动态专家、权限、隔离、消息、状态、持续运行、恢复、长期记忆、独立验证、Minority Report、真黑盒、ERP领域边界、资源保护及实施路线，并直接回答项目负责人提出的15项问题。

本任务是PM-001/D-113的补充设计，不重开PM-001，也不实现AGENT-R2—R5。

## 2. 现场基线与冲突

任务输入中的HEAD、工作区、private同步和`PHASE4-TASK03`实施阶段已过期。现场核验为：

- 根仓库`main@2c8f8b2e224e4a9b0a2ec9e01a5998898ff95aaf`，本地`origin/main`跟踪差异ahead 203；未fetch/push。
- 项目负责人既有未跟踪`docs/ERP_CURRENT_STATUS_REPORT.md`存在；本任务保持不读、不改、不暂存、不提交。
- 源码为alpha.44、41/head0041；UAT仍alpha.42/0040。
- `PHASE4-TASK03`已经完成源码候选但保持`BLOCKED / OWNER_PRIORITY_HOLD / SOURCE_READY / HOLDOUT_REVALIDATION_REQUIRED / RELEASE_NOT_AUTHORIZED`。
- PM-001、D-113和AGENT-R1已完成；R1实况在零DOING时返回IDLE，R2—R5未实施/未授权。

按“仓库中更新且权威事实优先”处理并在[设计索引](../ai-engineering/README.md#2-仓库事实与输入冲突)公开记录冲突。

## 3. 允许范围

- 读取AGENTS和项目权威文档、当前PHASE4任务、D-111/D-112、0035/0040/0041、产品架构/测试/权限/流程；
- 创建`docs/ai-engineering/*.md`和本任务文档；
- 更新MASTER、TASKS、PROJECT_CONTEXT、ROADMAP、DECISIONS、STATUS、CHANGELOG及PM-001历史设计的指针；
- 运行不写生产数据的低资源Markdown/仓库/Python基线验证；
- 创建一个聚焦文档commit。

## 4. 禁止范围

- 不修改业务、测试、Schema/Migration、API/Service/UI、package、Evaluator、数据集、报告、部署配置或Agent Runtime；
- 不创建Control Store、daemon、`.ai-team/`、worktree、测试数据库或产品AI数据；
- 不执行正式holdout、模型调用、build、Compose restart、UAT/生产访问、Migration、部署或业务写；
- 不访问或清理真实资料、日志、备份、protected volumes及项目负责人未跟踪文件；
- 不fetch/push、不改upstream/remote/Git拓扑，不自动启动R1.5、R2或恢复`PHASE4-TASK03`。

## 5. 交付物

交付物为[多智能体设计文档包](../ai-engineering/README.md)，包含架构、角色、编排、权限、消息、状态、记忆、资源、黑盒、失败恢复、ERP边界和实施路线。D-114仅记录为`PROPOSED / OWNER DECISION REQUIRED / IMPLEMENTATION NOT STARTED`。

## 6. 验收标准

- [x] 记录提示与现场事实冲突，并保留更高权威事实。
- [x] 明确4个常驻逻辑职责、0个常驻LLM和动态任务角色/专家机制。
- [x] 明确READ/WRITE/EXECUTE/DATABASE/NETWORK/GIT/DEPLOY及能力等级。
- [x] 明确单写者、独立审查/QA、安全与领域否决、Minority Report。
- [x] 定义含指定12字段的Message Contract和证据要求。
- [x] 定义TASKS四态与交付状态机、合法/非法迁移、真BLOCKED。
- [x] 定义检查点、RESULT_UNKNOWN、中断恢复、deadlock和有界retry/failure。
- [x] 定义无源码/.git的真黑盒及实际11角色驱动的动态Persona。
- [x] 严格区分研发Agent控制数据和D-112产品五表。
- [x] 给出native-first R1.5 MVP及R2—R5路线，且不自动实施。
- [x] 在设计索引直接回答15项问题。
- [x] 完成Markdown内部链接、diff、敏感信息、范围、基线测试和资源终态验证并记录结果。
- [x] 创建不含用户既有未跟踪文件的独立Git commit。

## 7. 状态序列

项目负责人本次指令直接授权单一文档任务；在零DOING起点按`PM-002 TODO → DOING → DONE`收口。期间`PHASE4-TASK03`保持BLOCKED且没有产品工作项；任务结束回到零DOING/IDLE，不自动认领任何下一任务。

## 8. 完成验证

- 13份设计文件齐全；新增/修改Markdown本地链接102个、断链0，指定Message字段完整，`git diff --check`、docs-only范围及增量敏感模式扫描通过。
- 0035/0040/0041 SHA-256仍分别为`d64ec733…c9714`、`b6781c94…a5a93`、`676626b9…71bf2`，package仍alpha.44；业务/测试代码差异为0，未创建Runtime目录。
- AGENT-R1专项测试24/24通过；仓库实况为`IDLE`、errors空，只保留8个既有一致历史重复终态行warning。
- Python `SELF_TEST_OK`、`SMOKE_TEST_OK`、loopback且`--no-backup`的`GO_LIVE_CHECK_OK`通过。
- 既有Node 22镜像在断网、源码只读、1 CPU、1,280 MiB、heap 768 MiB且单容器串行条件下完成`npm test` 3/3与lint退出0；临时容器已自动清零。
- 任务起点/终态available约`2.3/2.2 GiB`、Swap`354/354 MiB`、根盘可用`17/17 GiB`、Load`0.17/0.17/0.16`→`2.35/1.04/0.44`；终态1分钟Load低于4且未持续触线，内核OOM0，四服务restart0/OOM false。根目录`docker compose ps`因没有Compose配置失败关闭，未读取env或换路径重试；既有容器改由只读stats/inspect核验。
- 独立提交消息为`docs: complete ERP multi-agent operating design`，实际SHA以`git log`为准；不push。
