# SELFHOST-LANDING-TASK06：按既有模板离线整理内部物料库

## 状态

- 任务状态：`DONE / OFFLINE EXPORT / REVIEW REQUIRED`
- 开始日期：2026-07-30（Asia/Shanghai）
- 完成日期：2026-07-30（Asia/Shanghai）
- 负责人：Codex（实施与验证）、项目负责人（提供源表和目标）
- 依赖：`SELFHOST-LANDING-TASK02`、`SELFHOST-PHASE6-TASK01`

## 用户目标

读取 `/opt/erp/shujvbiao/` 中的表格，以其中 `moban.xlsx` 已整理格式为标准，把目录内现有来源物料整理为一份可审阅、可追溯的内部物料库工作簿。

## 固定输入

- 标准模板：`/opt/erp/shujvbiao/moban.xlsx`。第一张 `原BOM` 只作原版对照，第二张 `Sheet1` 是项目负责人确认的整理后标准，也是本任务唯一字段模板。
- 来源文件：目录中除模板和本任务输出外的 8 份 XLS/XLSX 文件。
- 既有映射证据：`SELFHOST-LANDING-TASK02` 在仓库外 root-only 目录保存的逐行分类、映射和已导入物料 payload；只读复用，不重新猜测正式编码。

所有输入在处理前后都必须核对文件名、大小和 SHA-256；不得修改源文件。

## 输出合同

业务数据结果保存为 `/opt/erp/shujvbiao/内部物料库.xlsx`，权限收紧为 root-only，不进入 Git。工作簿至少包含：

1. `标准BOM明细`：严格使用第二张 `Sheet1` 首行的 13 列，汇总所有可解释的 BOM/项目明细；模板后续分段发生的列位变化统一映射回这 13 列，需求和购买数量使用可审阅公式，未知数量保持空白。
2. `内部物料库`：以稳定内部记录为主，一行一物料；正式 ERP 编码只允许沿用既有导入结果，不在本任务中生成新正式编码。
3. `待确认`：列出规格缺失、身份冲突、数量或项目语义不完整及其他不能安全落库的来源行。
4. `来源映射`：记录每个库内条目与原文件、Sheet、原始行号的关系。
5. `来源与说明`：记录源文件摘要、模板合同、统计和边界。

## 业务与安全规则

- 严格执行 D-024、D-025、D-035、D-079：名称不作为唯一身份，缺项或冲突时 fail closed，不做模糊归并。
- 供应商料号、制造商料号和客户料号只能作为映射证据，不能成为内部主键。
- 替代料只作为待审证据，不自动形成正式替代关系。
- 不复制来源备注、联系电话或其他自由正文到结果表；供应商字段只采用明确供应商列或已整理模板值。
- 不写 PostgreSQL、SQLite、D1，不运行 Migration，不部署或重启服务，不改变现有正式物料/BOM。
- 源表、生成结果、逐行业务报告和任何可能含业务数据的文件都不得提交 Git。

## 验收标准

1. 模板 13 列逐字匹配，工作簿可正常打开，无宏、外链或来源公式注入。
2. 8 份来源文件均进入来源清单；所有非归档物料来源行都能在正式物料来源映射或待确认清单中找到。
3. 既有正式 ERP 编码唯一且保持不变；待确认条目不分配正式编码。
4. 严格身份相同的来源才允许归并，并保留逐行追溯；缺失或冲突不静默丢弃。
5. 标准明细的需求/购买公式、非负数量、来源行号和统计报告通过自动校验。
6. 输出不含电话号码、完整备注、凭据或生产连接信息，文件权限为 `0600`。
7. 适用单元测试、只读工作簿验收、`git diff --check`、敏感信息和最终范围检查通过。
8. 更新 `MASTER.md`、`TASKS.md`、`PROJECT_CONTEXT.md`、`CHANGELOG.md`、`STATUS.md`，并创建不含业务数据的独立 Git 提交。

## 明确排除

- 不把本次离线整理结果直接导入或覆盖正式数据库。
- 不审核、批准、激活、冻结或合并正式物料。
- 不修改 Schema、Migration、API、页面、Compose 或生产配置。
- 不处理库存、采购、工单、财务或其他交易数据。

## 完成结果

- 固定结论：`OFFLINE INTERNAL MATERIAL LIBRARY CREATED — REVIEW REQUIRED`。
- 工作簿：`/opt/erp/shujvbiao/内部物料库.xlsx`，root:root `0600`，SHA-256 `01d0239afc9dd5650fb577457c7c2ef85978fd59e8ca1d1b30ff9ba6527b5fa0`。
- 详细验收：`docs/tasks/SELFHOST-LANDING-TASK06-COMPLETION.md`。
