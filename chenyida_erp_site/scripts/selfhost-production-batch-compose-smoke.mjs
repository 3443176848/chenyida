import assert from "node:assert/strict";
import { Pool } from "pg";

const base=process.env.ERP_SMOKE_BASE_URL||"http://web:3000",databaseUrl=process.env.DATABASE_URL||"",phase=process.env.ERP_TASK07_SMOKE_PHASE||"initial";
const main=/@postgres(?::5432)?\/chenyida_erp$/i.test(databaseUrl),restore=phase==="restore"&&/@postgres(?::5432)?\/chenyida_erp_task07_restore$/i.test(databaseUrl),preflight=phase==="preflight"&&/@postgres(?::5432)?\/production_batch_acceptance_test_task07$/i.test(databaseUrl);
if(process.env.ERP_TASK07_ACCEPTANCE_CONFIRM!=="PARALLEL_SYNTHETIC_ONLY"||process.env.ERP_ENV==="production"||(!main&&!restore&&!preflight))throw new Error("TASK07 smoke requires the explicitly confirmed parallel synthetic, preflight or fixed restore database");
async function cleanup(pool){const admin=(await pool.query("select username from app_users where role='admin' and is_active order by username limit 1")).rows[0]?.username;if(!admin)throw new Error("active admin required");const tables=(await pool.query("select tablename from pg_tables where schemaname='public' and tablename not in ('schema_migrations','app_meta','app_users') order by tablename")).rows.map(row=>`"${String(row.tablename).replaceAll('"','""')}"`);if(tables.length)await pool.query(`truncate table ${tables.join(',')} restart identity cascade`);await pool.query("delete from app_users where username<>$1",[admin]);return admin;}

if(phase==="initial"||phase==="preflight"){
 if(preflight)process.env.ERP_TASK07_PREFLIGHT_CONFIRM="PARALLEL_SYNTHETIC_ONLY";
 process.env.ERP_TASK05_ACCEPTANCE_CONFIRM="PARALLEL_SYNTHETIC_ONLY";process.env.ERP_TASK05_SMOKE_PHASE="initial";
 const fixture=await import("./selfhost-production-nonconformance-compose-fixture.mjs"),{pool,provision,seed}=fixture;
 try{
  let baseline=(await pool.query("select (select count(*)::int from schema_migrations) migrations,(select count(*)::int from app_users where is_active) users,(select count(*)::int from production_work_orders) work_orders")).rows[0];if(preflight&&baseline.users===0){const{hashPassword}=await import("../app/lib/identity-selfhost/password.ts");await pool.query("insert into app_users(username,display_name,role,password_hash,is_active,must_change_password,version) values('task07admin','TASK07 隔离管理员','admin',$1,true,false,1)",[await hashPassword("Preflight9!Task07")]);baseline={...baseline,users:1};}assert.deepEqual(baseline,{migrations:31,users:1,work_orders:0});
  const admin=(await pool.query("select username from app_users where role='admin' and is_active order by username limit 1")).rows[0].username,adminActor=await provision("admin"),production=await provision("production"),warehouse=await provision("warehouse"),quality=await provision("quality"),manager=await provision("manager"),engineering=await provision("engineering"),planning=await provision("planning"),users={production:production.username,warehouse:warehouse.username,manager:manager.username,engineering:engineering.username},refs=await seed(admin,users);
  const workOrder=(await production.client.get(`/api/work-orders/${refs.workOrderId}`)).payload.data.header;
  await planning.client.post("/api/production/batch-sets",{work_order_id:refs.workOrderId,expected_work_order_version:Number(workOrder.version)},403);
  const createKey="task07-create-batch-set",created=await adminActor.client.post("/api/production/batch-sets",{work_order_id:refs.workOrderId,expected_work_order_version:Number(workOrder.version)},201,createKey),replayed=await adminActor.client.post("/api/production/batch-sets",{work_order_id:refs.workOrderId,expected_work_order_version:Number(workOrder.version)},201,createKey);assert.equal(replayed.response.headers.get("Idempotency-Replayed"),"true");
  const setId=Number(created.payload.data.id);let setVersion=Number(created.payload.data.version);
  const aAdded=await adminActor.client.post(`/api/production/batch-sets/${setId}/batches`,{planned_qty:"4",expected_batch_set_version:setVersion},201);setVersion=Number(aAdded.payload.batch_set_version);const batchA=aAdded.payload.data;
  await adminActor.client.post(`/api/production/batch-sets/${setId}/release`,{expected_version:setVersion},409);
  const bAdded=await adminActor.client.post(`/api/production/batch-sets/${setId}/batches`,{planned_qty:"6",expected_batch_set_version:setVersion},201);setVersion=Number(bAdded.payload.batch_set_version);const batchB=bAdded.payload.data;
  const released=await adminActor.client.post(`/api/production/batch-sets/${setId}/release`,{expected_version:setVersion},200);assert.match(released.payload.data.canonical_digest,/^[0-9a-f]{64}$/);assert.equal(released.payload.data.batches.reduce((sum,row)=>sum+Number(row.planned_qty),0),10);
  await adminActor.client.patch(`/api/production/batches/${batchA.id}`,{planned_qty:"3",expected_version:Number(batchA.version),expected_batch_set_version:Number(released.payload.data.version)},409);
  async function projection(op){return(await pool.query("select p.version operation_version,w.* from production_work_order_operation_projections p join production_operation_wip_projections w on w.operation_projection_id=p.id where p.snapshot_operation_id=$1",[op.snapshot_operation_id])).rows[0];}
  async function executeBatch(browser,batch,op,quantity){const current=await projection(op),dispatch=await browser.post("/api/production/operation-execution/dispatch",{snapshot_operation_id:Number(op.snapshot_operation_id),production_batch_id:Number(batch.id),quantity:String(quantity),assigned_operator:production.username,expected_operation_version:Number(current.operation_version)},201),run=dispatch.payload.data,start=await browser.post(`/api/production/operation-runs/${run.id}/start`,{expected_version:Number(run.version)},200),report=await browser.post(`/api/production/operation-runs/${run.id}/reports`,{expected_version:Number(start.payload.data.version),processed_qty:String(quantity),good_qty:String(quantity),scrap_qty:"0"},201);return{run_id:Number(run.id),run_report_id:Number(report.payload.data.report.id),run_version:Number(report.payload.data.run.version)};}
  await executeBatch(adminActor.client,batchA,refs.operations[0],4);let mountProjection=await projection(refs.operations[1]);await production.client.post("/api/production/operation-execution/dispatch",{snapshot_operation_id:Number(refs.operations[1].snapshot_operation_id),production_batch_id:Number(batchB.id),quantity:"1",assigned_operator:production.username,expected_operation_version:Number(mountProjection.operation_version)},409);
  await executeBatch(adminActor.client,batchA,refs.operations[1],4);const aReflow=await executeBatch(adminActor.client,batchA,refs.operations[2],4);
  const aInspection=await quality.client.post("/api/quality-inspections",{inspection_type:"IPQC",operation_run_report_id:aReflow.run_report_id,inspected_qty:"4",passed_qty:"4",failed_qty:"0",responsible_stage:"Batch A REFLOW",results:[{characteristic:"Batch A",result:"PASS"}]},201),aDisposition=await manager.client.post(`/api/quality-inspections/${aInspection.payload.data.id}/dispositions`,{expected_version:1,disposition_code:"RELEASE",release_qty:"4",reason:"Batch A 放行 4"},200);await manager.client.post(`/api/quality-inspections/${aInspection.payload.data.id}/close`,{expected_version:Number(aDisposition.payload.data.version),reason:"关闭 Batch A IPQC"},200);const aAoi=await executeBatch(adminActor.client,batchA,refs.operations[3],4);
  await executeBatch(production.client,batchB,refs.operations[0],6);await executeBatch(production.client,batchB,refs.operations[1],6);const bReflow=await executeBatch(production.client,batchB,refs.operations[2],6);
  const original=await quality.client.post("/api/quality-inspections",{inspection_type:"IPQC",operation_run_report_id:bReflow.run_report_id,inspected_qty:"6",passed_qty:"4",failed_qty:"2",responsible_stage:"Batch B REFLOW 原检",results:[{characteristic:"合格",result:"PASS"},{characteristic:"回流焊不良",result:"FAIL"}],defects:[{result_line_no:2,defect_type:"回流焊不良",severity:"MAJOR",quantity:"2",description:"Batch B 返工"}]},201),disposed=await manager.client.post(`/api/quality-inspections/${original.payload.data.id}/dispositions`,{expected_version:1,disposition_code:"RELEASE",release_qty:"4",reason:"Batch B 原检放行 4"},200),closed=await manager.client.post(`/api/quality-inspections/${original.payload.data.id}/close`,{expected_version:Number(disposed.payload.data.version),reason:"关闭 Batch B 原检"},200),ncr=(await quality.client.post(`/api/quality/inspections/${original.payload.data.id}/nonconformance`,{expected_version:Number(closed.payload.data.version)},201)).payload.data;
  const v1=(await quality.client.post(`/api/quality/nonconformances/${ncr.id}/rework-requests`,{target_snapshot_operation_id:Number(refs.operations[2].snapshot_operation_id),quantity:"2",reason:"Batch B v1"},201)).payload.data,submitted1=(await quality.client.post(`/api/quality/rework-requests/${v1.id}/submit`,{expected_version:Number(v1.version)},200)).payload.data;await production.client.post(`/api/production/rework-requests/${v1.id}/return`,{expected_version:Number(submitted1.version),reason:"补充返工说明"},200);
  const v2=(await quality.client.post(`/api/quality/nonconformances/${ncr.id}/rework-requests`,{supersedes_request_id:Number(v1.id),target_snapshot_operation_id:Number(refs.operations[2].snapshot_operation_id),quantity:"2",reason:"Batch B v2 REFLOW"},201)).payload.data,submitted2=(await quality.client.post(`/api/quality/rework-requests/${v2.id}/submit`,{expected_version:Number(v2.version)},200)).payload.data;await production.client.post(`/api/production/rework-requests/${v2.id}/accept`,{expected_version:Number(submitted2.version)},200);
  const request=(await production.client.get("/api/production/rework-requests?page_size=100")).payload.data.find(row=>Number(row.id)===Number(v2.id));await warehouse.client.post(`/api/production/rework-requests/${v2.id}/dispatch`,{expected_execution_version:Number(request.execution_version),quantity:"2",assigned_operator:production.username},403);const reworkDispatch=await production.client.post(`/api/production/rework-requests/${v2.id}/dispatch`,{expected_execution_version:Number(request.execution_version),quantity:"2",assigned_operator:production.username},201,"task07-rework-dispatch"),reworkReplay=await production.client.post(`/api/production/rework-requests/${v2.id}/dispatch`,{expected_execution_version:Number(request.execution_version),quantity:"2",assigned_operator:production.username},201,"task07-rework-dispatch");assert.equal(reworkReplay.response.headers.get("Idempotency-Replayed"),"true");const reworkRun=reworkDispatch.payload.data;assert.equal(Number(reworkRun.production_batch_id),Number(batchB.id));const reworkStart=await production.client.post(`/api/production/operation-runs/${reworkRun.id}/start`,{expected_version:Number(reworkRun.version)},200),reworkReport=await production.client.post(`/api/production/operation-runs/${reworkRun.id}/reports`,{expected_version:Number(reworkStart.payload.data.version),processed_qty:"2",good_qty:"2",scrap_qty:"0"},201);
  const bAoiFour=await executeBatch(manager.client,batchB,refs.operations[3],4);
  const reinspection=await quality.client.post("/api/quality-inspections",{inspection_type:"IPQC",operation_run_report_id:Number(reworkReport.payload.data.report.id),inspected_qty:"2",passed_qty:"2",failed_qty:"0",responsible_stage:"Batch B 返工复检",results:[{characteristic:"返工复检",result:"PASS"}]},201),reworkDisposition=await manager.client.post(`/api/quality-inspections/${reinspection.payload.data.id}/dispositions`,{expected_version:1,disposition_code:"RELEASE",release_qty:"2",reason:"Batch B 返工放行 2"},200);await manager.client.post(`/api/quality-inspections/${reinspection.payload.data.id}/close`,{expected_version:Number(reworkDisposition.payload.data.version),reason:"关闭 Batch B 复检"},200);const bAoiTwo=await executeBatch(manager.client,batchB,refs.operations[3],2);
  async function current(){return(await pool.query("select wo.version work_order_version,w.version final_output_version from production_work_orders wo join production_work_order_operation_projections p on p.work_order_id=wo.id and p.next_snapshot_operation_id is null join production_operation_wip_projections w on w.operation_projection_id=p.id where wo.id=$1",[refs.workOrderId])).rows[0];}
  let versions=await current();await production.client.post("/api/production/reports",{work_order_id:refs.workOrderId,expected_work_order_version:Number(versions.work_order_version),expected_final_output_version:Number(versions.final_output_version),final_output_allocations:[{operation_run_report_id:aAoi.run_report_id,quantity:"1"},{operation_run_report_id:bAoiFour.run_report_id,quantity:"1"}]},409);
  versions=await current();const reportA=await production.client.post("/api/production/reports",{work_order_id:refs.workOrderId,expected_work_order_version:Number(versions.work_order_version),expected_final_output_version:Number(versions.final_output_version),final_output_allocations:[{operation_run_report_id:aAoi.run_report_id,quantity:"4"}],remark:"Batch A Final Output 4"},201);versions=await current();const reportB=await production.client.post("/api/production/reports",{work_order_id:refs.workOrderId,expected_work_order_version:Number(versions.work_order_version),expected_final_output_version:Number(versions.final_output_version),final_output_allocations:[{operation_run_report_id:bAoiFour.run_report_id,quantity:"4"},{operation_run_report_id:bAoiTwo.run_report_id,quantity:"2"}],remark:"Batch B Final Output 6"},201);
  versions=await current();await warehouse.client.post("/api/production/completions",{work_order_id:refs.workOrderId,expected_version:Number(versions.work_order_version),expected_balance_version:0,reason:"混合 Batch 拒绝",allocations:[{report_id:Number(reportA.payload.data.id),quantity:"1",expected_report_version:1},{report_id:Number(reportB.payload.data.id),quantity:"1",expected_report_version:1}]},409);
  versions=await current();await warehouse.client.post("/api/production/completions",{work_order_id:refs.workOrderId,expected_version:Number(versions.work_order_version),expected_balance_version:0,reason:"Batch A 入库 4",allocations:[{report_id:Number(reportA.payload.data.id),quantity:"4",expected_report_version:1}]},201);versions=await current();await warehouse.client.post("/api/production/completions",{work_order_id:refs.workOrderId,expected_version:Number(versions.work_order_version),expected_balance_version:1,reason:"Batch B 入库 6",allocations:[{report_id:Number(reportB.payload.data.id),quantity:"6",expected_report_version:1}]},201);
  const genealogyA=(await production.client.get(`/api/production/batches/${batchA.id}/genealogy`)).payload.data,genealogyB=(await production.client.get(`/api/production/batches/${batchB.id}/genealogy`)).payload.data;assert.equal(Number(genealogyA.batch.id),Number(batchA.id));assert.equal(Number(genealogyB.batch.id),Number(batchB.id));assert.ok(genealogyB.rework_runs.every(row=>Number(row.production_batch_id)===Number(batchB.id)));assert.equal(genealogyB.inventory_links.every(row=>row.lot_code===""),true);
  const facts=(await pool.query(`select wo.planned_qty::text planned,wo.reported_qty::text reported,wo.good_qty::text good,wo.scrap_qty::text scrap,wo.completed_qty::text completed,wo.status,(select array_agg(b.planned_qty::text order by b.id) from production_batches b where b.batch_set_id=$2) batch_plans,(select canonical_digest from production_batch_sets where id=$2) digest,(select array_agg(r.processed_qty::text order by r.id) from production_operation_runs r where r.production_batch_id=$3 and r.snapshot_operation_id=$4) b_reflow_processing,(select inspected_qty::text||'/'||passed_qty::text||'/'||failed_qty::text||'/'||released_qty::text from quality_inspections where id=$5) original_ipqc,(select inspected_qty::text||'/'||passed_qty::text||'/'||failed_qty::text||'/'||released_qty::text from quality_inspections where id=$6) reinspection,(select array_agg(r.reported_qty::text order by rb.production_batch_id) from production_reports r join production_report_batches rb on rb.production_report_id=r.id where r.work_order_id=wo.id) reports,(select array_agg(l.on_hand_delta::text order by l.id) from inventory_ledger_entries l where l.material_id=$7) ledger,(select bool_and(l.lot_code='') from inventory_ledger_entries l where l.material_id=$7) blank_lots,(select on_hand_qty::text from inventory_stock_balances where material_id=$7 and location_code='MAIN' and lot_code='') balance,(select count(*)::int from production_operation_run_input_allocations a join production_operation_runs s on s.id=a.source_run_id join production_operation_runs t on t.id=a.run_id where s.production_batch_id is distinct from t.production_batch_id) cross_batch,(select count(*)::int from quality_inspections where inspection_type='FQC') fqc,(select count(*)::int from sales_shipments) shipments,(select count(*)::int from sales_financial_source_entries) sales_sources,(select count(*)::int from finance_documents where doc_type='AR') ar,(select count(*)::int from finance_settlements) settlements from production_work_orders wo where wo.id=$1`,[refs.workOrderId,setId,batchB.id,refs.operations[2].snapshot_operation_id,original.payload.data.id,reinspection.payload.data.id,refs.finishedId])).rows[0];
  assert.deepEqual(facts,{planned:"10.000000",reported:"10.000000",good:"10.000000",scrap:"0.000000",completed:"10.000000",status:"COMPLETED",batch_plans:["4.000000","6.000000"],digest:released.payload.data.canonical_digest,b_reflow_processing:["6.000000","2.000000"],original_ipqc:"6.000000/4.000000/2.000000/4.000000",reinspection:"2.000000/2.000000/0.000000/2.000000",reports:["4.000000","6.000000"],ledger:["4.000000","6.000000"],blank_lots:true,balance:"10.000000",cross_batch:0,fqc:0,shipments:0,sales_sources:0,ar:0,settlements:0});
  const batches=(await production.client.get(`/api/production/batches?work_order_id=${refs.workOrderId}`)).payload.rows;assert.deepEqual(batches.map(row=>[row.batch_code,row.batch_status,row.released_qty,row.completed_qty]).sort((a,b)=>String(a[0]).localeCompare(String(b[0]))),[[batchA.batch_code,"COMPLETED","4.000000","4.000000"],[batchB.batch_code,"COMPLETED","6.000000","6.000000"]]);const byCode=(await production.client.get(`/api/production/batches?code=${encodeURIComponent(batchA.batch_code)}`)).payload.rows;assert.equal(byCode.length,1);
  const orderRefs=(await pool.query(`select p.id product_id,pv.id product_version_id,bv.id bom_version_id,m.id finished_material_id,u.id finished_unit_id from products p join product_versions pv on pv.product_id=p.id and pv.status='RELEASED' join bom_versions bv on bv.product_version_id=pv.id and bv.status='RELEASED' join material_master m on m.internal_material_code='CYD-T05-FG' join units u on u.id=m.base_unit_id where p.product_code='PRD-T05'`)).rows[0],orderDraft=(await production.client.post("/api/work-orders",{...orderRefs,planned_qty:"1",owner:production.username},201)).payload.data;await production.client.post(`/api/work-orders/${orderDraft.id}/release`,{expected_version:Number(orderDraft.version)},200);const orderRequirement=(await pool.query("select * from production_material_requirements where work_order_id=$1",[orderDraft.id])).rows[0],rawBalance=(await pool.query("select b.version from inventory_stock_balances b join material_master m on m.id=b.material_id where m.internal_material_code='CYD-T05-RAW' and b.location_code='MAIN' and b.lot_code='' ")).rows[0];await warehouse.client.post("/api/production/material-issues",{work_order_id:Number(orderDraft.id),reason:"ORDER 模式实际 HTTP 回归",lines:[{requirement_id:Number(orderRequirement.id),quantity:"1",expected_requirement_version:Number(orderRequirement.version),expected_balance_version:Number(rawBalance.version)}]},201);const orderOperation=(await pool.query("select p.id,p.snapshot_operation_id,p.version from production_work_order_operation_projections p join production_work_order_routing_snapshot_operations op on op.id=p.snapshot_operation_id where p.work_order_id=$1 order by op.sequence_no limit 1",[orderDraft.id])).rows[0],orderDispatch=await production.client.post("/api/production/operation-execution/dispatch",{snapshot_operation_id:Number(orderOperation.snapshot_operation_id),quantity:"1",assigned_operator:production.username,expected_operation_version:Number(orderOperation.version)},201),orderRun=orderDispatch.payload.data,orderStart=await production.client.post(`/api/production/operation-runs/${orderRun.id}/start`,{expected_version:Number(orderRun.version)},200);await production.client.post(`/api/production/operation-runs/${orderRun.id}/reports`,{expected_version:Number(orderStart.payload.data.version),processed_qty:"1",good_qty:"1",scrap_qty:"0"},201);const orderSummary=(await production.client.get(`/api/work-orders/${orderDraft.id}/batch-summary`)).payload.data;assert.equal(orderSummary.mode,"ORDER");assert.equal((await pool.query("select production_batch_id from production_operation_runs where work_order_id=$1",[orderDraft.id])).rows[0].production_batch_id,null);
  const dashboard=(await manager.client.get("/api/management-dashboard")).payload;for(const code of ["draft-batch-sets","released-waiting-batches","batch-wip","batch-quality-hold","batch-rework","batch-final-output","completed-batches"])assert.ok(dashboard.metrics.some(item=>item.code===code),code);
  for(const path of ["/production/batches","/production/dispatch","/production/operations","/production/wip","/production/reporting","/warehouse/production-completions","/quality/production","/quality/nonconformances","/production/rework-requests"])assert.equal((await fetch(`${base}${path}`)).status,200,path);
  console.info(JSON.stringify({ok:true,phase,work_order_id:refs.workOrderId,order_mode_work_order_id:Number(orderDraft.id),batch_set_id:setId,batch_a:{id:Number(batchA.id),code:batchA.batch_code,quantity:4},batch_b:{id:Number(batchB.id),code:batchB.batch_code,quantity:6,original:"4+2",rework:2,reinspection:2,processing:8},digest:released.payload.data.canonical_digest,facts,genealogy:{a_runs:genealogyA.normal_runs.length,b_normal_runs:genealogyB.normal_runs.length,b_rework_runs:genealogyB.rework_runs.length}}));
 }finally{await pool.end();}
}else{
 const pool=new Pool({connectionString:databaseUrl,max:4,application_name:"task07-batch-smoke-verify"});try{
  if(["verify","restart","restore"].includes(phase)){const facts=(await pool.query(`select (select count(*)::int from schema_migrations) migrations,(select count(*)::int from production_batch_sets where status='RELEASED') released_sets,(select array_agg(planned_qty::text order by id) from production_batches) batch_plans,(select count(*)::int from production_operation_runs where production_batch_id is not null and run_kind='NORMAL') normal_runs,(select count(*)::int from production_operation_runs where production_batch_id is not null and run_kind='REWORK') rework_runs,(select count(*)::int from production_operation_runs where production_batch_id is null and run_kind='NORMAL') order_runs,(select count(*)::int from quality_inspections) inspections,(select count(*)::int from production_nonconformances) ncrs,(select count(*)::int from production_reports) reports,(select count(*)::int from production_completions) completions,(select array_agg(on_hand_delta::text order by id) from inventory_ledger_entries where material_id=(select id from material_master where internal_material_code='CYD-T05-FG')) ledger,(select bool_and(lot_code='') from inventory_ledger_entries where material_id=(select id from material_master where internal_material_code='CYD-T05-FG')) blank_lots,(select on_hand_qty::text from inventory_stock_balances where material_id=(select id from material_master where internal_material_code='CYD-T05-FG') and location_code='MAIN' and lot_code='') balance,(select count(*)::int from audit_log) audits,(select count(*)::int from idempotency_keys) idempotency`)).rows[0];assert.deepEqual({...facts,audits:0,idempotency:0},{migrations:31,released_sets:1,batch_plans:["4.000000","6.000000"],normal_runs:9,rework_runs:1,order_runs:1,inspections:3,ncrs:1,reports:2,completions:2,ledger:["4.000000","6.000000"],blank_lots:true,balance:"10.000000",audits:0,idempotency:0});assert.ok(facts.audits>0&&facts.idempotency>0);if(phase!=="restore")assert.equal((await fetch(`${base}/api/health`)).status,200);console.info(JSON.stringify({ok:true,phase,durable:true,...facts}));}
  else if(phase==="cleanup"){const admin=await cleanup(pool),facts=(await pool.query("select (select count(*)::int from schema_migrations) migrations,(select count(*)::int from app_users where is_active) users,(select count(*)::int from production_batch_sets) batch_sets,(select count(*)::int from production_work_orders) work_orders,(select count(*)::int from audit_log) audits,(select count(*)::int from idempotency_keys) idempotency")).rows[0];assert.deepEqual(facts,{migrations:31,users:1,batch_sets:0,work_orders:0,audits:0,idempotency:0});console.info(JSON.stringify({ok:true,phase,admin,...facts}));}else throw new Error(`unsupported phase ${phase}`);
 }finally{await pool.end();}
}
