"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {api,ErpApiError} from "../../../public/erp/api-client.js";import "../../procurement/sourcing/sourcing.css";
type Row={work_order_id:number;work_order_code:string;work_order_status:string;planned_qty:string;product_code:string;product_name:string;routing_state:string;routing_code?:string;routing_version_code?:string;first_sequence_no?:number;first_operation_code?:string;first_operation_name?:string;first_work_center_code?:string};
export default function DispatchPage(){const[rows,setRows]=useState<Row[]>([]),[error,setError]=useState("");useEffect(()=>{void api<{rows:Row[]}>("/api/production/dispatch").then(v=>setRows(v.rows)).catch(e=>setError(e instanceof ErpApiError?e.message:"读取失败"));},[]);return <main className="sourcing-shell"><header className="sourcing-header"><div><Link href="/">← 经营工作台</Link><h1>生产派工准备</h1><p>只读展示工单工艺快照与第一待执行工序；本任务不提供开工、完工或报工按钮。</p><p>迁移前工单保持 LEGACY_UNSTRUCTURED，不从 process_stage 猜测路线。</p></div></header><section className="sourcing-panel"><h2>工单工艺</h2>{rows.map(row=><article className="sourcing-card" key={row.work_order_id}><b>{row.work_order_code} · {row.product_code} {row.product_name}</b><small>{row.work_order_status} · planned {row.planned_qty} · {row.routing_state}</small>{row.routing_state==="STRUCTURED"?<p>{row.routing_code} / {row.routing_version_code} · 第一工序 {row.first_sequence_no} {row.first_operation_code} {row.first_operation_name} · {row.first_work_center_code}</p>:<p>{row.routing_state}</p>}</article>)}</section>{error?<div className="sourcing-state sourcing-error">{error}</div>:null}</main>}
