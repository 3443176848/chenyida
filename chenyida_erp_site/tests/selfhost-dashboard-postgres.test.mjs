import assert from "node:assert/strict";import {randomUUID} from "node:crypto";import {mkdtemp,rm,writeFile} from "node:fs/promises";import os from "node:os";import path from "node:path";import test from "node:test";import {Pool} from "pg";
import {PostgresDashboardRepository} from "../app/lib/dashboard-selfhost/repository.ts";import {DashboardService} from "../app/lib/dashboard-selfhost/service.ts";import {permissionsForRole} from "../app/lib/identity-selfhost/permissions.ts";
const databaseUrl=process.env.TEST_DASHBOARD_DATABASE_URL;if(!databaseUrl||!/dashboard_test/i.test(databaseUrl))throw new Error("isolated TEST_DASHBOARD_DATABASE_URL containing dashboard_test is required");const pool=new Pool({connectionString:databaseUrl,application_name:"dashboard-integration-test"});const actor=role=>({username:`${role}01`,role,permissions:permissionsForRole(role)});const directory=await mkdtemp(path.join(os.tmpdir(),"cyd-dashboard-pg-"));const statusFile=path.join(directory,"latest.json");const service=new DashboardService(new PostgresDashboardRepository(pool),statusFile);
test.beforeEach(async()=>{await pool.query("truncate material_master,material_categories,background_jobs restart identity cascade")});test.after(async()=>{await pool.end();await rm(directory,{recursive:true,force:true})});
test("read-only repeatable-read snapshot aggregates authoritative empty and material facts",async()=>{const empty=await service.summary(actor("operations"));assert.equal(empty.authority,"Node/PostgreSQL");assert.equal(empty.consistency,"REPEATABLE_READ_READ_ONLY");assert.equal(empty.inventory_quantity_aggregated,false);assert.equal(empty.groups.material.active_materials,0);assert.equal(empty.groups.finance.ar_balance,"0");const category=await pool.query("insert into material_categories(category_code,category_name_cn,category_level,created_by,updated_by,request_id) values('DASH','看板测试',1,'test','test',$1) returning id",[randomUUID()]);await pool.query("insert into material_master(internal_material_code,standard_name,category_id,base_uom,material_status,procurement_type,inventory_type,inspection_type,environmental_requirement,source_type,last_modified_by,created_by,updated_by,request_id) values('DASH-001','看板物料',$1,'PCS','ACTIVE','PURCHASED','STOCK','NONE','ROHS','MANUAL','test','test','test',$2)",[category.rows[0].id,randomUUID()]);await pool.query("insert into background_jobs(id,type,idempotency_key,payload,status) values($1,'test.dashboard','dashboard-test','{}','FAILED')",[randomUUID()]);const result=await service.management(actor("operations"));assert.equal(result.summary.groups.material.active_materials,1);assert.equal(result.summary.total_items,1);assert.equal(result.summary.groups.operations.failed_jobs,1);assert.ok(result.risks.some(item=>item.code==="BACKGROUND_JOB_FAILED"));assert.equal(result.summary.groups.operations.migrations[0].version,"0030_production_rework_execution.sql");assert.equal(result.recent_activity.length,0);});
test("trusted verification file is independent from PostgreSQL business facts",async()=>{for(const role of ["manager","operations"])await assert.rejects(service.backup(actor(role)),e=>e.code==="PERMISSION_DENIED");const hash="b".repeat(64);await writeFile(statusFile,JSON.stringify({schema_version:1,result:"VERIFIED",backup_id:"dashboard-test",created_at:"2026-07-25T01:00:00Z",verified_at:"2026-07-25T02:00:00Z",application_version:"0.1.0-alpha.10",git_commit:"b".repeat(40),migration_head:"0013_finance.sql",artifacts:{postgresql_dump:{file:"postgresql.dump",sha256:hash,bytes:10},uploads:{file:"uploads.tar.gz",sha256:hash,bytes:10,entries:0},attachments:{file:"attachments.tar.gz",sha256:hash,bytes:10,entries:0}}}));const result=await service.backup(actor("admin"));assert.equal(result.verification_status,"VERIFIED");assert.equal(result.current_migration.version,"0030_production_rework_execution.sql");});
