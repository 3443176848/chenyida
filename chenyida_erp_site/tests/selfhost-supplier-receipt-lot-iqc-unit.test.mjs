import assert from"node:assert/strict";import test from"node:test";import{permissionsForRole}from"../app/lib/identity-selfhost/permissions.ts";import{receiptLines,supplierLotCode}from"../app/lib/procurement-selfhost/rules.ts";
test("supplier lot input is normalized and fail-closed",()=>{assert.equal(supplierLotCode("  sup-a-20260727  "),"SUP-A-20260727");assert.equal(supplierLotCode(undefined),null);for(const value of["   ","A".repeat(65),"SUP A","供应商批次"])assert.throws(()=>supplierLotCode(value),/供应商 Lot/);assert.deepEqual(receiptLines([{purchase_order_line_id:1,quantity:"10",expected_line_version:1,expected_balance_version:0,supplier_lot_code:" lot/a-1 "}])[0],{purchaseOrderLineId:1,quantity:"10",expectedLineVersion:1,expectedBalanceVersion:0,supplierLotCode:"LOT/A-1"})});
test("ten roles preserve receiving, IQC disposition and read separation",()=>{assert.ok(permissionsForRole("warehouse").includes("procurement.receiving.receive"));assert.ok(!permissionsForRole("warehouse").includes("quality.disposition"));assert.ok(permissionsForRole("quality").includes("quality.disposition"));assert.ok(!permissionsForRole("quality").includes("procurement.receiving.receive"));assert.ok(permissionsForRole("purchase").includes("inventory.lot.read"));assert.ok(!permissionsForRole("purchase").includes("quality.disposition"));assert.ok(permissionsForRole("production").includes("inventory.lot.read"));assert.ok(!permissionsForRole("production").includes("production.issue")||!permissionsForRole("production").includes("inventory.adjust"))});
