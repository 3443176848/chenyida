import assert from "node:assert/strict";import {readFile} from "node:fs/promises";import test from "node:test";
const page=await readFile(new URL("../app/page.tsx",import.meta.url),"utf8");const workbench=await readFile(new URL("../app/_components/erp-workbench.tsx",import.meta.url),"utf8");const legacy=await readFile(new URL("../public/erp/app.js",import.meta.url),"utf8");const html=await readFile(new URL("../public/erp/index.html",import.meta.url),"utf8");
test("root is native, authenticated, non-production and independently loads cards",()=>{assert.doesNotMatch(page,/<iframe/i);assert.match(page,/ErpWorkbench/);assert.doesNotMatch(workbench,/<iframe/i);for(const route of ["/api/session","/api/summary","/api/management-dashboard","/api/backup-governance","/api/setup","/api/login","/api/me/password","/api/logout"])assert.match(workbench,new RegExp(route));assert.match(workbench,/非生产开发基线/);assert.match(workbench,/loadCard/);assert.doesNotMatch(workbench,/Promise\.all/);});
test("legacy remains explicit, has allowlisted deep links, and no browser restore control",()=>{assert.match(workbench,/legacy 兼容业务台/);assert.match(legacy,/LEGACY_TABS = new Set/);assert.match(legacy,/requestedLegacyTab/);for(const tab of ["partners","bom","purchase","production","sales","quality","finance"])assert.match(legacy,new RegExp(`\"${tab}\"`));assert.doesNotMatch(html,/id="createBackupBtn"/);assert.doesNotMatch(legacy,/data-restore-backup|async function restoreBackup|async function createBackup/);assert.match(html,/禁止浏览器在线原地恢复/);});
test("native root does not issue the legacy refreshAll 23-request batch",()=>{assert.doesNotMatch(workbench,/refreshAll|\/api\/purchase-order-lines|\/api\/production-reports/);const match=legacy.match(/async function refreshAll\(\)[\s\S]*?Promise\.all\(\[([\s\S]*?)\]\)/);assert.ok(match);assert.equal([...match[1].matchAll(/api\("\/api\//g)].length,23);});
