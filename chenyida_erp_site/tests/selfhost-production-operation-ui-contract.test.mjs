import assert from "node:assert/strict";import {readFile} from "node:fs/promises";import test from "node:test";
const read=path=>readFile(new URL(path,import.meta.url),"utf8");
test("native production operation pages use snapshot operation identity",async()=>{const dispatch=await read("../app/production/dispatch/page.tsx"),operations=await read("../app/production/operations/page.tsx"),wip=await read("../app/production/wip/page.tsx");for(const [source,path] of [[dispatch,"/production/dispatch"],[operations,"/production/operations"],[wip,"/production/wip"]])assert.ok(source.length>500,path);assert.match(dispatch,/snapshot_operation_id/);assert.doesNotMatch(dispatch,/process_stage/);assert.match(operations,/operation-runs/);assert.match(operations,/工序报工/);assert.match(wip,/final_output_available_qty/);assert.match(wip,/不是 MAIN 库存 Ledger/);});
test("browser client protects operation writes with csrf and idempotency",async()=>{const client=await read("../public/erp/api-client.js");assert.match(client,/operation-execution\/dispatch/);assert.match(client,/operation-runs/);assert.match(client,/Idempotency-Key/);assert.match(client,/X-CSRF-Token/);});
