import assert from "node:assert/strict";import {readFile} from "node:fs/promises";import test from "node:test";
const read=path=>readFile(new URL(path,import.meta.url),"utf8");
test("native routing pages expose controlled workflows",async()=>{const [wc,routing,dispatch]=await Promise.all([read("../app/operations/work-centers/page.tsx"),read("../app/engineering/routings/page.tsx"),read("../app/production/dispatch/page.tsx")]);for(const token of ["工作中心","production.work_center.manage","ACTIVE","INACTIVE"])assert.match(wc,new RegExp(token));for(const token of ["产品工艺路线","提交审核","production.routing.review","发布","退回"])assert.match(routing,new RegExp(token));for(const token of ["生产派工准备","第一待执行工序","不提供开工、完工或报工按钮","LEGACY_UNSTRUCTURED"])assert.match(dispatch,new RegExp(token));assert.doesNotMatch(dispatch,/>开工<|>完工<|>报工</);});
